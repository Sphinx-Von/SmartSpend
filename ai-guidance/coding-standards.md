# Coding Standards

Backend (Flask):
- Validate all request inputs
- Centralize business logic in services
- Return consistent API response structure

Frontend (React):
- Keep UI separate from business logic
- Use typed API responses
- Avoid storing derived state unnecessarily

Database:
- Use relational constraints where applicable
- Prevent negative expense amounts
- Enforce required fields