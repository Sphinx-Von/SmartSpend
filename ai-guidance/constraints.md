# AI Constraints

When using AI, the following constraints were enforced:

- Do not modify business rules without explicit instruction
- Do not introduce new dependencies unless justified
- Keep separation between API layer, service layer, and models
- Prevent invalid states through validation
- Prefer simple and readable code over abstraction

All outputs were reviewed for:
- Correctness
- Security issues
- Unnecessary complexity